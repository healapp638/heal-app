import { StyleSheet } from 'react-native';

const style = (colors: any) =>
  StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    },
    logo: {
      width: 220,
      height: 108,
    },
  });

export default style;
